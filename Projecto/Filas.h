#pragma once
#include "DataFrame.h"
#pragma once
#include "Columnas.h"

template<typename T>
class Filas
{
private:

	int dx;

public:

	Filas(int _dx) : dx(_dx) { }

	string getVal(string nom) {
		// return this ->
	}
};